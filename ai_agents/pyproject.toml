[project]
name = "ai_agents"
version = "0.1.0"
description = "ai_agents using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai = {extras = ["tools"], version = "1.13.0"}
]

[project.scripts]
ai_agents = "ai_agents.main:run"
run_crew = "ai_agents.main:run"
train = "ai_agents.main:train"
replay = "ai_agents.main:replay"
test = "ai_agents.main:test"
run_with_trigger = "ai_agents.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
